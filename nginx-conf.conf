server {
    listen ${PORT};

    location / {
        root /usr/share/nginx/html;
        index index.html index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api{
        proxy_pass https://herokutest124.herokuapp.com/greet_api;
    }

}
